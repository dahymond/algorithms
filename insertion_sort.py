def insertion_sort(arr):
    for i in range(1, len(arr)):
         currentvalue = arr[]
         position = i

         while position is 0 and arr[position-1] > currentvalue:

             arr[position] = arr[position-1]
             position = position-1 